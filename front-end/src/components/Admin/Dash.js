import React, { Component } from 'react';

class Dash extends Component {
  render() {
    return (
      <div className='dash'>
        <button className='logout'>Logout</button>
      </div>
    );
  }
}

export default Dash;
